import React from 'react'

const OffsetWindow = ({ subAgent }) => (
  <div className="offset-window-container-wrapper">
    <div className="offset-window-container">{subAgent}</div>
  </div>
)

export default OffsetWindow
